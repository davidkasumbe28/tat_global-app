{% comment %} import { Invoice } from "../@types/types";
import { formatDate } from "../utils/invoice.utils";

interface ResetEmailContent {
  email: string;
  firstName: string;
  tokenId?: string;
  link?: string;
} {% endcomment %}


{% comment %} export function resetEmailHTML(content: ResetEmailContent): string {
  return `
<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Titre</title>
    <style>
      :root {
        --background: oklch(1 0 0);
        --foreground: oklch(0.145 0 0);
        --gray: oklch(0.205 0 0);
        --gray-foreground: oklch(0.985 0 0);
        --primary: #d4a574;
        --primary-foreground: oklch(0.985 0 0);
        --primary-dark: #b8934a;
        --primary-dark-foreground: oklch(0.145 0 0);
        --secondary: oklch(0.97 0 0);
        --secondary-foreground: oklch(0.205 0 0);
        --muted: oklch(0.97 0 0);
        --muted-foreground: oklch(82.344% 0.00009 271.152);
        --accent: oklch(89.143% 0.0001 271.152);
        --accent-foreground: oklch(0.205 0 0);
        --destructive: oklch(0.577 0.245 27.325);
        --destructive-foreground: oklch(0.577 0.245 27.325);
      }
      body {
        font-family: "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
        min-height: 100vh;
        padding: 40px;
        background-color: oklch(1 0 0);
        color: oklch(0.145 0 0);
      }
      .container {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        border: 1.5px solid #e5e7eb;
        border-radius: 10px;
        padding: 20px 40px;
      }

      header {
        display: flex;
        justify-content: center;
        align-items: start;
        gap: 20px;
        width: 100%;
        margin-bottom: 20px;
        border-bottom: 2px solid #b8934a;
        padding: 10px 0px;
      }

      .company-info {
        display: flex;
        flex-direction: column;
        justify-content: start;
        align-items: start;
        padding: 0px 10px;
      }
      .company-name {
        display: flex;
        align-items: center;
        gap: 10px;
        margin: 0px 0px 10px 0px;
      }
      .company-name .logo {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 18px;
        height: 18px;
        padding: 5px;
        background-color: oklch(0.145 0 0);
        color: oklch(0.985 0 0);
        border-radius: 8px;
      }
      .company-name .logo span {
        margin: 0;
        font-size: 18px;
      }

      .company-name h1 {
        margin: 0;
        font-size: 20px;
      }

      .body {
        display: block;
        width: 100%;
        height: auto;
      }

      .content {
        display: flex;
        flex-direction: column;
        width: 100%;
        gap: 20px;
      }

      .button {
        height: auto;
        border: 2px solid #b8934a;
        border-radius: 10px;
        padding: 15px 20px;
        margin: 20px 0px;
        background-color: #b8934a5b;
      }

      .button:hover {
        background-color: #b8934a;
        border: 0px;
      }

      .button a {
        text-decoration: none;
        color: black;
        font-size: 16px;
      }

      .box {
        height: auto;
        border: 2px solid #b8934a;
        border-radius: 10px;
        padding: 15px 20px;
        margin: 20px 0px;
        background-color: #b8934a5b;
        color: black;
        font-size: 16px;
      }

      .footer {
        width: 100%;
        margin-top: 40px;
        padding: 10px;
        border-top: 1px solid #e5e7eb;
        text-align: center;
        font-size: 14px;
        color: oklch(57.61% 0.00007 271.152);
      }

      @media screen and (max-width: 768px) {
        body {
          padding: 20px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <header>
        <div class="company-info">
          <div class="company-name">
            <div class="logo">
              <span>TG</span>
            </div>
            <h1>TAT GLOBAL</h1>
          </div>
        </div>
      </header>

      <div class="body">
        <div class="titre">
          <h3>Plus qu'une étape pour modifier votre mot de passe</h3>
        </div>

        <div class="content">
          <section>
            <p>Bonjour ${content.firstName},</p>
            <p>
              Nous avons reçu votre demande pour la récupération de votre
              compte. Cliquez sur ce button ci-dessous pour continuer.
            </p>
          </section>


          <section
            style="display: flex; justify-content: center; align-items: center"
          >
            <strong class="box">${content.tokenId}</strong>
          </section>

          <section
            style="display: flex; justify-content: center; align-items: center"
          >
            <span style="font-size: 14px; color: oklch(66.326% 0.00008 271.152)"
              >Ce lien expirera dans 30 minutes.</span
            >
          </section>

          <section>
            <p>
              Pour des raisons de sécurité, ne partagez jamais ce message avec qui
              que ce soit.
            </p>
            <p>
              Si vous n'êtes pas à l'origine de cette demande, veuillez ignorer
              cet e-mail.
            </p>
          </section>

          <section>
            <p>
              Merci, <br />
              Sécurité TAT GLOBAL
            </p>
          </section>
        </div>
      </div>

      <div class="footer">
        <p>Ce message a été envoyé à ${content.email}</p>
        <p>
          Pour contribuer à la protection de votre compte, veuiller ne pas
          transférer cet e-mail.
          <a href="http://" target="_blank" rel="noopener noreferrer"
            >En savoir plus</a
          >
        </p>
        <p>Merci d'avoir choisi TAT GLOBAL</p>
      </div>
    </div>
  </body>
</html>
  `;
}

export function invoiceEmailHTML(
  invoice: Invoice,
  companyInfo: Record<string, string>,
): string {
  const subtotal = invoice.items.reduce((sum, item) => sum + item.subtotal, 0);
  const taxAmount = invoice.items.reduce(
    (sum, item) => sum + item.subtotal * item.taxRate,
    0,
  );
  const total = subtotal + taxAmount;

  return `
    <!DOCTYPE html>
    <html lang="fr">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Facture ${invoice.invoiceNumber}</title>
      <style>
        body {
          font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
          color: #333;
          line-height: 1.6;
          margin: 0;
          padding: 20px;
          background: white;
        }
        .container {
          max-width: 900px;
          margin: 0 auto;
          border: 1px solid #e5e7eb;
          padding: 40px;
        }
        header {
          display: flex;
          justify-content: space-between;
          align-items: flex-start;
          margin-bottom: 40px;
          border-bottom: 2px solid #f59e0b;
          padding-bottom: 20px;
        }
        .company-info h1 {
          margin: 0;
          color: #d97706;
          font-size: 28px;
        }
        .company-info p {
          margin: 5px 0;
          color: #666;
          font-size: 12px;
        }
        .invoice-details {
          text-align: right;
        }
        .invoice-details p {
          margin: 5px 0;
          font-size: 12px;
        }
        .invoice-number {
          font-size: 24px;
          font-weight: bold;
          color: #d97706;
          margin: 10px 0;
        }
        .invoice-status {
          display: inline-block;
          padding: 5px 10px;
          background: #dcfce7;
          color: #166534;
          border-radius: 4px;
          font-size: 12px;
          font-weight: bold;
        }
        .client-info {
          display: grid;
          grid-template-columns: 1fr 1fr;
          gap: 40px;
          margin: 30px 0;
          padding: 20px 0;
        }
        .client-info h3 {
          margin: 0 0 10px 0;
          font-size: 12px;
          font-weight: bold;
          color: #666;
        }
        .client-info p {
          margin: 3px 0;
          font-size: 12px;
        }
        .items-table {
          width: 100%;
          border-collapse: collapse;
          margin: 30px 0;
        }
        .items-table thead {
          background: #f3f4f6;
          border-top: 2px solid #e5e7eb;
          border-bottom: 2px solid #e5e7eb;
        }
        .items-table th {
          padding: 12px;
          text-align: left;
          font-weight: 600;
          font-size: 12px;
          color: #374151;
        }
        .items-table td {
          padding: 12px;
          border-bottom: 1px solid #e5e7eb;
          font-size: 12px;
        }
        .items-table tbody tr:hover {
          background: #f9fafb;
        }
        .text-right {
          text-align: right;
        }
        .totals {
          display: flex;
          justify-content: flex-end;
          margin: 30px 0;
        }
        .totals-box {
          width: 300px;
        }
        .total-row {
          display: flex;
          justify-content: space-between;
          padding: 8px 0;
          border-bottom: 1px solid #e5e7eb;
          font-size: 12px;
        }
        .total-row.total {
          border-top: 2px solid #d97706;
          border-bottom: 2px solid #d97706;
          margin-top: 10px;
          padding: 12px 0;
          font-weight: bold;
          font-size: 14px;
        }
        .notes {
          margin: 30px 0;
          padding: 20px;
          background: #f9fafb;
          border-left: 4px solid #d97706;
          font-size: 12px;
        }
        .footer {
          margin-top: 40px;
          padding-top: 20px;
          border-top: 1px solid #e5e7eb;
          text-align: center;
          font-size: 10px;
          color: #999;
        }
        @media print {
          body {
            padding: 0;
          }
          .container {
            border: none;
            box-shadow: none;
          }
        }
      </style>
    </head>
    <body>
      <div class="container">
        <header>
          <div class="company-info">
            <h1>${companyInfo.name}</h1>
            <p>${companyInfo.address}</p>
            <p>${companyInfo.city}</p>
            <p>Tél: ${companyInfo.phone}</p>
            <p>Email: ${companyInfo.email}</p>
            <p>SIRET: ${companyInfo.siret}</p>
          </div>
          <div class="invoice-details">
            <div class="invoice-number">${invoice.invoiceNumber}</div>
            <div class="invoice-status">FACTURE</div>
            <p><strong>Date d'émission:</strong> ${formatDate(invoice.issueDate)}</p>
            <p><strong>Échéance:</strong> ${formatDate(invoice.dueDate)}</p>
          </div>
        </header>

        <div class="client-info">
          <div>
            <h3>FACTURÉ À:</h3>
            <p>${invoice.items[0]?.productName || "Client"}</p>
            <p style="margin-top: 15px;">Merci pour votre achat</p>
          </div>
        </div>

        <table class="items-table">
          <thead>
            <tr>
              <th>Désignation</th>
              <th class="text-right">Quantité</th>
              <th class="text-right">Prix unitaire</th>
              <th class="text-right">TVA</th>
              <th class="text-right">Montant</th>
            </tr>
          </thead>
          <tbody>
            ${invoice.items
              .map(
                (item) => `
              <tr>
                <td>${item.productName}</td>
                <td class="text-right">${item.quantity}</td>
                <td class="text-right">${item.unitPrice.toFixed(2)}€</td>
                <td class="text-right">${(item.taxRate * 100).toFixed(0)}%</td>
                <td class="text-right">${item.subtotal.toFixed(2)}€</td>
              </tr>
            `,
              )
              .join("")}
          </tbody>
        </table>

        <div class="totals">
          <div class="totals-box">
            <div class="total-row">
              <span>Sous-total:</span>
              <span>${subtotal.toFixed(2)}€</span>
            </div>
            <div class="total-row">
              <span>TVA (20%):</span>
              <span>${taxAmount.toFixed(2)}€</span>
            </div>
            <div class="total-row total">
              <span>TOTAL:</span>
              <span>${total.toFixed(2)}€</span>
            </div>
          </div>
        </div>

        ${invoice.notes ? `<div class="notes"><strong>Notes:</strong> ${invoice.notes}</div>` : ""}

        <div class="footer">
          <p>Conditions de paiement: ${invoice.paymentTerms || "Net 30"}</p>
          <p>Merci d'avoir choisi TAT GLOBAL</p>
        </div>
      </div>
    </body>
    </html>
  `;
} {% endcomment %}


<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Titre</title>
    <style>
      :root {
        --background: oklch(1 0 0);
        --foreground: oklch(0.145 0 0);
        --gray: oklch(0.205 0 0);
        --gray-foreground: oklch(0.985 0 0);
        --primary: #d4a574;
        --primary-foreground: oklch(0.985 0 0);
        --primary-dark: #b8934a;
        --primary-dark-foreground: oklch(0.145 0 0);
        --secondary: oklch(0.97 0 0);
        --secondary-foreground: oklch(0.205 0 0);
        --muted: oklch(0.97 0 0);
        --muted-foreground: oklch(82.344% 0.00009 271.152);
        --accent: oklch(89.143% 0.0001 271.152);
        --accent-foreground: oklch(0.205 0 0);
        --destructive: oklch(0.577 0.245 27.325);
        --destructive-foreground: oklch(0.577 0.245 27.325);
      }
      body {
        font-family: "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
        min-height: 100vh;
        padding: 40px;
        background-color: oklch(1 0 0);
        color: oklch(0.145 0 0);
      }
      .container {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        border: 1.5px solid #e5e7eb;
        border-radius: 10px;
        padding: 20px 40px;
      }

      header {
        display: flex;
        justify-content: center;
        align-items: start;
        gap: 20px;
        width: 100%;
        margin-bottom: 20px;
        border-bottom: 2px solid #b8934a;
        padding: 10px 0px;
      }

      .company-info {
        display: flex;
        flex-direction: column;
        justify-content: start;
        align-items: start;
        padding: 0px 10px;
      }
      .company-name {
        display: flex;
        align-items: center;
        gap: 10px;
        margin: 0px 0px 10px 0px;
      }
      .company-name .logo {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 18px;
        height: 18px;
        padding: 5px;
        background-color: oklch(0.145 0 0);
        color: oklch(0.985 0 0);
        border-radius: 8px;
      }
      .company-name .logo span {
        margin: 0;
        font-size: 18px;
      }

      .company-name h1 {
        margin: 0;
        font-size: 20px;
      }

      .body {
        display: block;
        width: 100%;
        height: auto;
      }

      .content {
        display: flex;
        flex-direction: column;
        width: 100%;
        gap: 20px;
      }

      .button {
        height: auto;
        border: 2px solid #b8934a;
        border-radius: 10px;
        padding: 15px 20px;
        margin: 20px 0px;
        background-color: #b8934a5b;
      }

      .button:hover {
        background-color: #b8934a;
        border: 0px;
      }

      .button a {
        text-decoration: none;
        color: black;
        font-size: 16px;
      }

      .box {
        height: auto;
        border: 2px solid #b8934a;
        border-radius: 10px;
        padding: 15px 20px;
        margin: 20px 0px;
        background-color: #b8934a5b;
        color: black;
        font-size: 16px;
      }

      .footer {
        width: 100%;
        margin-top: 40px;
        padding: 10px;
        border-top: 1px solid #e5e7eb;
        text-align: center;
        font-size: 14px;
        color: oklch(57.61% 0.00007 271.152);
      }

      @media screen and (max-width: 768px) {
        body {
          padding: 20px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <header>
        <div class="company-info">
          <div class="company-name">
            <div class="logo">
              <span>TG</span>
            </div>
            <h1>TAT GLOBAL</h1>
          </div>
        </div>
      </header>

      <div class="body">
        <div class="titre">
          <h3>Plus qu'une étape pour modifier votre mot de passe</h3>
        </div>

        <div class="content">
          <section>
            <p>Bonjour ${content.firstName},</p>
            <p>
              Nous avons reçu votre demande pour la récupération de votre
              compte. Cliquez sur ce button ci-dessous pour continuer.
            </p>
          </section>


          <section
            style="display: flex; justify-content: center; align-items: center"
          >
            <strong class="box">${content.tokenId}</strong>
          </section>

          <section
            style="display: flex; justify-content: center; align-items: center"
          >
            <span style="font-size: 14px; color: oklch(66.326% 0.00008 271.152)"
              >Ce lien expirera dans 30 minutes.</span
            >
          </section>

          <section>
            <p>
              Pour des raisons de sécurité, ne partagez jamais ce message avec qui
              que ce soit.
            </p>
            <p>
              Si vous n'êtes pas à l'origine de cette demande, veuillez ignorer
              cet e-mail.
            </p>
          </section>

          <section>
            <p>
              Merci, <br />
              Sécurité TAT GLOBAL
            </p>
          </section>
        </div>
      </div>

      <div class="footer">
        <p>Ce message a été envoyé à ${content.email}</p>
        <p>
          Pour contribuer à la protection de votre compte, veuiller ne pas
          transférer cet e-mail.
          <a href="http://" target="_blank" rel="noopener noreferrer"
            >En savoir plus</a
          >
        </p>
        <p>Merci d'avoir choisi TAT GLOBAL</p>
      </div>
    </div>
  </body>
</html>
  `;
}

export function invoiceEmailHTML(
  invoice: Invoice,
  companyInfo: Record<string, string>,
): string {
  const subtotal = invoice.items.reduce((sum, item) => sum + item.subtotal, 0);
  const taxAmount = invoice.items.reduce(
    (sum, item) => sum + item.subtotal * item.taxRate,
    0,
  );
  const total = subtotal + taxAmount;

  return `
    <!DOCTYPE html>
    <html lang="fr">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Facture ${invoice.invoiceNumber}</title>
      <style>
        body {
          font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
          color: #333;
          line-height: 1.6;
          margin: 0;
          padding: 20px;
          background: white;
        }
        .container {
          max-width: 900px;
          margin: 0 auto;
          border: 1px solid #e5e7eb;
          padding: 40px;
        }
        header {
          display: flex;
          justify-content: space-between;
          align-items: flex-start;
          margin-bottom: 40px;
          border-bottom: 2px solid #f59e0b;
          padding-bottom: 20px;
        }
        .company-info h1 {
          margin: 0;
          color: #d97706;
          font-size: 28px;
        }
        .company-info p {
          margin: 5px 0;
          color: #666;
          font-size: 12px;
        }
        .invoice-details {
          text-align: right;
        }
        .invoice-details p {
          margin: 5px 0;
          font-size: 12px;
        }
        .invoice-number {
          font-size: 24px;
          font-weight: bold;
          color: #d97706;
          margin: 10px 0;
        }
        .invoice-status {
          display: inline-block;
          padding: 5px 10px;
          background: #dcfce7;
          color: #166534;
          border-radius: 4px;
          font-size: 12px;
          font-weight: bold;
        }
        .client-info {
          display: grid;
          grid-template-columns: 1fr 1fr;
          gap: 40px;
          margin: 30px 0;
          padding: 20px 0;
        }
        .client-info h3 {
          margin: 0 0 10px 0;
          font-size: 12px;
          font-weight: bold;
          color: #666;
        }
        .client-info p {
          margin: 3px 0;
          font-size: 12px;
        }
        .items-table {
          width: 100%;
          border-collapse: collapse;
          margin: 30px 0;
        }
        .items-table thead {
          background: #f3f4f6;
          border-top: 2px solid #e5e7eb;
          border-bottom: 2px solid #e5e7eb;
        }
        .items-table th {
          padding: 12px;
          text-align: left;
          font-weight: 600;
          font-size: 12px;
          color: #374151;
        }
        .items-table td {
          padding: 12px;
          border-bottom: 1px solid #e5e7eb;
          font-size: 12px;
        }
        .items-table tbody tr:hover {
          background: #f9fafb;
        }
        .text-right {
          text-align: right;
        }
        .totals {
          display: flex;
          justify-content: flex-end;
          margin: 30px 0;
        }
        .totals-box {
          width: 300px;
        }
        .total-row {
          display: flex;
          justify-content: space-between;
          padding: 8px 0;
          border-bottom: 1px solid #e5e7eb;
          font-size: 12px;
        }
        .total-row.total {
          border-top: 2px solid #d97706;
          border-bottom: 2px solid #d97706;
          margin-top: 10px;
          padding: 12px 0;
          font-weight: bold;
          font-size: 14px;
        }
        .notes {
          margin: 30px 0;
          padding: 20px;
          background: #f9fafb;
          border-left: 4px solid #d97706;
          font-size: 12px;
        }
        .footer {
          margin-top: 40px;
          padding-top: 20px;
          border-top: 1px solid #e5e7eb;
          text-align: center;
          font-size: 10px;
          color: #999;
        }
        @media print {
          body {
            padding: 0;
          }
          .container {
            border: none;
            box-shadow: none;
          }
        }
      </style>
    </head>
    <body>
      <div class="container">
        <header>
          <div class="company-info">
            <h1>${companyInfo.name}</h1>
            <p>${companyInfo.address}</p>
            <p>${companyInfo.city}</p>
            <p>Tél: ${companyInfo.phone}</p>
            <p>Email: ${companyInfo.email}</p>
            <p>SIRET: ${companyInfo.siret}</p>
          </div>
          <div class="invoice-details">
            <div class="invoice-number">${invoice.invoiceNumber}</div>
            <div class="invoice-status">FACTURE</div>
            <p><strong>Date d'émission:</strong> ${formatDate(invoice.issueDate)}</p>
            <p><strong>Échéance:</strong> ${formatDate(invoice.dueDate)}</p>
          </div>
        </header>

        <div class="client-info">
          <div>
            <h3>FACTURÉ À:</h3>
            <p>${invoice.items[0]?.productName || "Client"}</p>
            <p style="margin-top: 15px;">Merci pour votre achat</p>
          </div>
        </div>

        <table class="items-table">
          <thead>
            <tr>
              <th>Désignation</th>
              <th class="text-right">Quantité</th>
              <th class="text-right">Prix unitaire</th>
              <th class="text-right">TVA</th>
              <th class="text-right">Montant</th>
            </tr>
          </thead>
          <tbody>
            ${invoice.items
              .map(
                (item) => `
              <tr>
                <td>${item.productName}</td>
                <td class="text-right">${item.quantity}</td>
                <td class="text-right">${item.unitPrice.toFixed(2)}€</td>
                <td class="text-right">${(item.taxRate * 100).toFixed(0)}%</td>
                <td class="text-right">${item.subtotal.toFixed(2)}€</td>
              </tr>
            `,
              )
              .join("")}
          </tbody>
        </table>

        <div class="totals">
          <div class="totals-box">
            <div class="total-row">
              <span>Sous-total:</span>
              <span>${subtotal.toFixed(2)}€</span>
            </div>
            <div class="total-row">
              <span>TVA (20%):</span>
              <span>${taxAmount.toFixed(2)}€</span>
            </div>
            <div class="total-row total">
              <span>TOTAL:</span>
              <span>${total.toFixed(2)}€</span>
            </div>
          </div>
        </div>

        ${invoice.notes ? `<div class="notes"><strong>Notes:</strong> ${invoice.notes}</div>` : ""}

        <div class="footer">
          <p>Conditions de paiement: ${invoice.paymentTerms || "Net 30"}</p>
          <p>Merci d'avoir choisi TAT GLOBAL</p>
        </div>
      </div>
    </body>
    </html>
