const user: {
  IN_PROGRESS: string;
  CHECKED: string;
  ONLINE: string;
  OFFLINE: string;
} = {
  IN_PROGRESS: "in progress",
  CHECKED: "checked",
  ONLINE: "online",
  OFFLINE: "offline",
};

const status = {
  user,
};

export default status;
